---
spring:
  application:
    name: "start-renovate"
server:
  servlet:
    context-path: "/api"
openai:
  base-url: "https://api.openai.com/v1"
  api-key: "${OPENAI_API_KEY}"
  model: "gpt-5-mini"
  maxOutputTokens: 800
