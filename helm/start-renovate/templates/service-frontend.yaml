apiVersion: v1
kind: Service
metadata:
  name: {{ include "start-renovate.frontend.fullname" . }}
  labels:
    {{- include "start-renovate.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "start-renovate.frontend.selectorLabels" . | nindent 4 }}
